;Exercise 2.25.  Give combinations of cars and cdrs that will pick 7 from
;each of the following lists:

(car (cdr (car (cdr (cdr below)))))
(1 3 (5 7) 9)

(car (car below))
((7))

(car (cdr (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr below))))))))))))
(1 (2 (3 (4 (5 (6 7))))))

